[dependencies]
klu-sys = { path = "../klu-sys" }

[lib]
crate-type = ["cdylib", "rlib"]
name = "klujax_ffi"

[package]
description = "XLA typed-FFI handlers exposing KLU to JAX, statically linking SuiteSparse"
edition.workspace = true
license.workspace = true
name = "klujax-ffi"
version.workspace = true
