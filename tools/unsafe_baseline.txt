# Unsafe-code baseline (work.md hardening plan)
#
# Metric: unsafe *operations* — `unsafe { ... }` blocks + `unsafe impl` — with
# `//` comments stripped (see tools/unsafe_budget.sh). Declaring an `unsafe fn`
# contract (FFI boundary / trait method) is not counted as an operation.
# The budget in tools/unsafe_budget.txt only ever goes down.
#
# Progressive totals (unsafe-operation metric):
#   Stage 0 baseline: 164   (162 `unsafe {` blocks + 2 `unsafe impl`)
#   after Stage 1:      ?   (Frame/Buffer decode consolidated)
#   after Stage 3:     69
#
# Earlier keyword-count snapshots (for reference):
#   baseline 222 `unsafe` tokens (132 handlers, 37 engine, 32 call_frame, …)
#   after Stage 1: 89 tokens
#   after Stage 3: 97 tokens (added `unsafe fn` trait-method *declarations*,
#                  which do not add operations; hence the metric change)
#
# Pristine modules (0 unsafe):
#   crates/klujax-ffi/src/lib.rs
#   crates/klu-sys/build.rs
#
# Guardrails enabled in Stage 0:
#   [workspace.lints.rust]   unsafe_op_in_unsafe_fn = "deny"
#   [workspace.lints.clippy] undocumented_unsafe_blocks = "deny"
#                            missing_safety_doc = "deny"
